The first line contains a single integer $t$ $(1 \leq t \leq 50)$ --- the number of testcases.

For each testcase, a single line contains two integers $a, b$ $(0 \leq a, b < 10^9 + 7)$.

It is guaranteed that $a,b$ before the modulo are less than or equal to $10^{100}$